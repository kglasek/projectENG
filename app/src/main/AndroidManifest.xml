<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projecteng">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/unjackiconproject"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/unjackiconproject_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".rewriteResult"></activity>
        <activity android:name=".textToSpeech" />
        <activity android:name=".rewriteSent" />
        <activity android:name=".oxfordDict" />
        <activity android:name=".quizresults" />
        <activity android:name=".quiz" />
        <activity android:name=".chooseex" />
        <activity android:name=".engczasy" />
        <activity android:name=".learnlan" />
        <activity
            android:name=".exercises"
            android:configChanges="orientation|screenSize|keyboardHidden" />
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>